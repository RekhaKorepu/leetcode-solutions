# 402. Remove K Digits
  
<br>**Problem:** https://leetcode.com/problems/remove-k-digits/<br>

**Difficulty:** Medium<br>
**Topics:** String, Stack, Greedy, Monotonic Stack<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-08-06 17:31 local time

**Runtime:** 27 ms (beats 30.237099999999998%)
**Memory:** 48 MB (beats 24.118299999999977%)


<!-- leetgit:submissionId=2096623541 codeHash=a80d7504660c62491e5b10d2eb7541e5968545b4ac6ea153a2da4ab95b5db61c notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
class Solution {
    public String removeKdigits(String num, int k) {
        Stack<Character> st= new Stack<>();
        for(int i=0;i<num.length();i++){
            while(!st.empty() && k>0 && (st.peek()-'0')> (num.charAt(i) - '0')){
                st.pop();
                k=k-1;
            }
            st.push(num.charAt(i));
        }
        // for this edge cases like "1234567" k=3
        while(k>0){
            st.pop();
            k--;
        }

        // nums length is same as the value of k
        if(st.empty()){
            return "0";
        }
       
        // Storing the result 

        StringBuilder sb = new StringBuilder();

        while (!st.empty()) {
            sb.append(st.pop());
        }

        sb.reverse();
        String res = sb.toString();

        int i = 0;
        while (i < res.length() && res.charAt(i) == '0') {
            i++;
        }

        if (i == res.length()) {
            return "0";
        }

        return res.substring(i);
        
    }
}
```
