# 113. Path Sum II
  
<br>**Problem:** https://leetcode.com/problems/path-sum-ii/<br>

**Difficulty:** Medium<br>
**Topics:** Backtracking, Tree, Depth-First Search, Binary Tree<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-08-04 17:12 local time

**Runtime:** 1 ms (beats 99.9691%)
**Memory:** 45.7 MB (beats 22.5821%)


<!-- leetgit:submissionId=2093915878 codeHash=5ae4b7958b699b80fb0bb9f38eb16467fb470e9e1f2db57020d31f54fbffa352 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
        public List<List<Integer>> pathSum(TreeNode root, int targetSum) {

        List<List<Integer>> result = new ArrayList<>();
        List<Integer> path = new ArrayList<>();

        dfs(root, targetSum, path, result);

        return result;
    }

    private void dfs(TreeNode root, int sum,
                     List<Integer> path,
                     List<List<Integer>> result) {

        if (root == null)
            return;

        path.add(root.val);

        int subsum = sum - root.val;

        if (subsum == 0 && root.left == null && root.right == null) {
            result.add(new ArrayList<>(path));
        }

        dfs(root.left, subsum, path, result);
        dfs(root.right, subsum, path, result);

        path.remove(path.size() - 1); // backtrack
    }

}
```
