# 954. Maximum Sum Circular Subarray
  
<br>**Problem:** https://leetcode.com/problems/maximum-sum-circular-subarray/<br>

**Difficulty:** Medium<br>
**Topics:** Array, Divide and Conquer, Dynamic Programming, Queue, Monotonic Queue<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-08-10 17:14 local time

**Runtime:** 6 ms (beats 79.6493%)
**Memory:** 51.2 MB (beats 23.816699999999972%)


<!-- leetgit:submissionId=2101476956 codeHash=4866f393fb9bded01b3c3e5852f072692730cba22eb13288efac9d518d0b4c15 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
class Solution {
    public int maxSubarraySumCircular(int[] nums) {

        int currMax = 0;
        int globalMax = Integer.MIN_VALUE;

        int currMin = 0;
        int globalMin = Integer.MAX_VALUE;

        int total = 0;

        for (int num : nums) {

            // Maximum subarray
            currMax = Math.max(num, currMax + num);
            globalMax = Math.max(globalMax, currMax);

            // Minimum subarray
            currMin = Math.min(num, currMin + num);
            globalMin = Math.min(globalMin, currMin);

            // Total sum
            total += num;
        }

        // All numbers are negative
        if (globalMax < 0) {
            return globalMax;
        }

        // Maximum of normal and circular subarray
        return Math.max(globalMax, total - globalMin);
    }
}
```
